# ============================================================
# 多智能体系统依赖（生产级）
# ============================================================

# 多智能体编排框架
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0

# LLM 容错（重试/退避）
tenacity>=8.2.0

# 联网搜索
tavily-python>=0.5.0

# 配置管理（类型安全）
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# Web 服务（异步）
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
# 多进程部署：gunicorn 作为进程管理器，配合 uvicorn worker 支持 ASGI 异步
gunicorn>=22.0.0

# 请求频率限制
slowapi>=0.1.9

# 认证
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# 数据库（异步 PostgreSQL）
asyncpg>=0.29.0

# 向量扩展（pgvector Python 辅助库）
pgvector>=0.3.0

# 分布式缓存（Redis，优先使用；不可用时回退内存缓存）
redis>=5.0.0

# 本地 Ollama 模型（视觉识别 + 嵌入）
ollama>=0.4.0


# 文档解析
pdfplumber>=0.11.0
python-docx>=1.1.0

# 监控
prometheus-fastapi-instrumentator>=7.0.0

# 可观测性（分布式追踪，可选启用）
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp-proto-grpc>=1.25.0

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
